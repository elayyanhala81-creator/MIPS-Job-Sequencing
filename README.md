# MIPS-Job-Sequencing
MIPS Assembly program that solves the Job Sequencing with Deadlines problem using a greedy scheduling algorithm
The program reads job data from a file, sorts jobs by profit, schedules them using a greedy algorithm, calculates the total profit, and saves the final results to an output file
